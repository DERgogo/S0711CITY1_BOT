# Example env file for local development. Do NOT store real secrets in the repo.
# Copy this to `.env` and fill values. Add `.env` to .gitignore.

# Your Telegram bot token from BotFather
BOT_TOKEN=your_bot_token_here

# Secret token used when setting webhook: should match `secret_token` in set_webhook
WEBHOOK_SECRET=replace_with_secret

# Optional: override path/URL for local tunnels (ngrok/cloudflare)
#WEBHOOK_PATH=/webhook/<token>
#WEBHOOK_URL=https://<your-public-url>/webhook/<token>
